name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
    
    - name: Restore dependencies
      run: dotnet restore
    
    - name: Build Core Projects
      run: |
        for project in src/**/*.csproj; do
          dotnet build "$project" --no-restore
        done
    
    - name: Build and Test
      run: |
        for project in test/**/*.csproj; do
          dotnet build "$project" --no-restore
        done
        for project in test/**/*.csproj; do
          dotnet test "$project" --no-build --verbosity normal --collect:"XPlat Code Coverage" --results-directory ./coverage
        done
    
    - name: Merge Coverage Reports
      if: always()
      run: |
        dotnet tool install --global dotnet-coverage
        dotnet-coverage merge --output coverage/merged/coverage.cobertura.xml --output-format cobertura "coverage/*/coverage.cobertura.xml"

    - name: Code Coverage Report
      if: always()
      uses: irongut/CodeCoverageSummary@v1.3.0
      with:
        filename: coverage/merged/coverage.cobertura.xml
        badge: true
        fail_below_min: false
        format: markdown
        hide_branch_rate: false
        hide_complexity: true
        indicators: true
        output: both
        thresholds: '60 80'
      continue-on-error: true
    
    - name: Add Coverage PR Comment
      if: always() && github.event_name == 'pull_request'
      uses: marocchino/sticky-pull-request-comment@v2
      with:
        recreate: true
        path: code-coverage-results.md
    
    - name: Write to Job Summary
      if: always()
      run: |
        if [ -f code-coverage-results.md ]; then
          cat code-coverage-results.md >> $GITHUB_STEP_SUMMARY
        fi

    - name: Send coverage to Coveralls
      if: always()
      uses: coverallsapp/github-action@v2
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        file: coverage/merged/coverage.cobertura.xml
        coverage-format: cobertura
      env:
        COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN }}
